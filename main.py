from sklearn.ensemble import IsolationForest
import pandas as pd
import sys


def read_dataset(file: str) -> pd.DataFrame:

    df = pd.DataFrame()
    return df

def issolation_forest(df: pd.DataFrame):

    return


if __name__ == "__main__":

    exit(0)